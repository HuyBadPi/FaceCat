import Feather from '@expo/vector-icons/Feather';
import * as React from 'react';

const Send = (props) => (
    <Feather name="send" size={24} color="#000000" fill="none" {...props} />
);

export default Send;