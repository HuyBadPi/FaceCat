import Ionicons from '@expo/vector-icons/Ionicons';
import * as React from 'react';

const Call = (props) => (
    <Ionicons name="call-outline" size={24} color="#000000" fill="none" {...props} />
);

export default Call;