import Ionicons from '@expo/vector-icons/Ionicons';
import * as React from 'react';

const Logout = (props) => (
    <Ionicons name="power" size={24} color="#000000" fill="none" {...props} />
);

export default Logout;