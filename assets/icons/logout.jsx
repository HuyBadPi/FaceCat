import MaterialIcons from '@expo/vector-icons/MaterialIcons';
import * as React from 'react';

const Logout = (props) => (
    <MaterialIcons name="logout" size={24} color="#000000" fill="none" {...props} />
);

export default Logout;