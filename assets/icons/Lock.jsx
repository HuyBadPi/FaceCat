import Feather from '@expo/vector-icons/Feather';
import * as React from 'react';

const Lock = (props) => (
    <Feather name="lock" size={24} color="#000000" fill="none" {...props} />
);

export default Lock;