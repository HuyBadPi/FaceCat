import Feather from '@expo/vector-icons/Feather';
import * as React from 'react';

const Video = (props) => (
    <Feather name="video" size={24} color="#000000" fill="none" {...props} />
);

export default Video;