import Feather from '@expo/vector-icons/Feather';
import * as React from 'react';

const Camera = (props) => (
    <Feather name="camera" size={24} color="#000000" fill="none" {...props} />
);

export default Camera;