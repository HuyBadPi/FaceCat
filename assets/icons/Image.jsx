import Ionicons from '@expo/vector-icons/Ionicons';
import * as React from 'react';

const Image = (props) => (
    <Ionicons name="image-outline" size={24} color="#000000" fill="none" {...props} />
);

export default Image;